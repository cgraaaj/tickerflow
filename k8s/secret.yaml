apiVersion: v1
kind: Secret
metadata:
  name: tickerflow-secret
  namespace: tickerflow
  labels:
    app: tickerflow
type: Opaque
stringData:
  DJANGO_SECRET_KEY: "CHANGE-ME-generate-a-long-random-string"
  DB_PASSWORD: "CHANGE-ME-your-db-password"
