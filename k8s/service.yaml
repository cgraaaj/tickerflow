apiVersion: v1
kind: Service
metadata:
  name: tickerflow-api
  namespace: tickerflow
  labels:
    app: tickerflow
    component: api
spec:
  type: ClusterIP
  selector:
    app: tickerflow
    component: api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
